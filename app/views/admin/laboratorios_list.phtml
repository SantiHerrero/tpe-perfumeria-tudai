<h1 class="page-title">Laboratorios</h1>

<?php if (empty($labs)): ?>
  <p>No hay laboratorios registrados actualmente.</p>
<?php else: ?>
  <div class="lab-list">
    <?php foreach($labs as $lab): ?>
      <div class="card lab-card">
        <h2 class="lab-name">
          <a href="<?= BASE_URL ?>?action=laboratorios/<?= $lab['id'] ?>">
            <?= htmlspecialchars($lab['nombre']) ?>
          </a>
        </h2>

        <p class="lab-desc"><?= htmlspecialchars($lab['descripcion'] ?: 'Sin descripción') ?></p>

        <p class="lab-fundador">
          <strong>Fundador:</strong> <?= htmlspecialchars($lab['fundador']) ?>
        </p>

        <p class="lab-pais">
          <strong>País:</strong> <?= htmlspecialchars($lab['pais']) ?>
        </p>

        <a class="btn" href="<?= BASE_URL ?>?action=laboratorios/<?= $lab['id'] ?>">
          Ver perfumes
        </a>
      </div>
    <?php endforeach; ?>
  </div>
<?php endif; ?>
